# NORBEAI backend — pinned dependencies.
# Install CPU PyTorch FIRST (see requirements-cpu.txt), then:
#   pip install -r requirements.txt

coqui-tts==0.27.5
faster-whisper==1.2.1
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1
